'use client';

import Menu from './components/Menu';
import ThemeToggle from './components/ThemeToggle';
import Process from './components/Process';
import { Faq5 } from './components/Faq5';
import { Stats12 } from './components/Stats12';
import { Footer } from './components/Footer';

export default function Home() {
  return (
    <>
      <style jsx>{`
        body {
          min-height: 200vh;
          overflow-y: auto;
          position: relative;
        }

        .content {
          position: relative;
          z-index: 2;
          display: flex;
          align-items: center;
          justify-content: center;
          height: 100vh;
          text-align: center;
        }

        .content h1 {
          font-size: clamp(2.5rem, 8vw, 5rem);
          font-weight: 600;
          letter-spacing: -0.03em;
          color: #f9f8fd;
          transition: color 300ms ease;
        }

        :global(body.light-mode) .content h1 {
          color: #000;
        }

        .hero-section {
          position: relative;
          z-index: 4;
          padding: 5rem 2rem;
          background: rgba(0, 0, 0, 0.3);
          backdrop-filter: blur(8px);
          -webkit-backdrop-filter: blur(8px);
          transition: background 300ms ease;
        }

        :global(body.light-mode) .hero-section {
          background: rgba(255, 255, 255, 0.3);
        }

        .hero-container {
          max-width: 1200px;
          margin: 0 auto;
          display: flex;
          flex-direction: column;
          align-items: center;
          gap: 2.5rem;
        }

        .hero-text {
          display: flex;
          flex-direction: column;
          gap: 1.75rem;
          width: 100%;
        }

        .hero-text h2 {
          font-size: clamp(3rem, 8vw, 6rem);
          font-weight: 600;
          line-height: 1.1;
          margin: 0;
          color: #ffffff;
          transition: color 300ms ease;
        }

        :global(body.light-mode) .hero-text h2 {
          color: #000000;
        }

        .hero-subheading {
          color: rgba(255, 255, 255, 0.5);
          transition: color 300ms ease;
        }

        :global(body.light-mode) .hero-subheading {
          color: rgba(0, 0, 0, 0.5) !important;
        }

        .hero-description {
          font-size: clamp(1rem, 2vw, 1.25rem);
          line-height: 1.6;
          margin: 0;
          color: rgba(255, 255, 255, 0.7);
          transition: color 300ms ease;
        }

        :global(body.light-mode) .hero-description {
          color: rgba(0, 0, 0, 0.7) !important;
        }

        .hero-buttons {
          display: flex;
          flex-wrap: wrap;
          gap: 1.75rem;
          margin-top: 1rem;
        }

        .hero-btn-primary {
          display: inline-flex;
          align-items: center;
          gap: 0.5rem;
          padding: 0.75rem 2rem;
          background: #bbffcc;
          color: #000;
          border-radius: 6px;
          text-decoration: none;
          font-weight: 500;
          transition: all 200ms ease;
          border: 1px solid #bbffcc;
        }

        .hero-btn-primary:hover {
          background: #a3ff99;
          transform: translateY(-2px);
          box-shadow: 0 4px 12px rgba(187, 255, 204, 0.3);
        }

        :global(body.light-mode) .hero-btn-primary {
          background: #bbffcc;
          color: #000;
          border-color: #bbffcc;
        }

        :global(body.light-mode) .hero-btn-primary:hover {
          background: #a3ff99;
          box-shadow: 0 4px 12px rgba(187, 255, 204, 0.4);
        }

        .hero-btn-secondary {
          display: inline-flex;
          align-items: center;
          padding: 0.75rem 1rem;
          color: #bbffcc;
          text-decoration: underline;
          font-weight: 500;
          transition: all 200ms ease;
        }

        .hero-btn-secondary:hover {
          opacity: 0.8;
          transform: translateY(-1px);
        }

        :global(body.light-mode) .hero-btn-secondary {
          color: #000;
        }

        .hero-mockup {
          position: relative;
          width: 100%;
          max-width: 450px;
          margin-top: 2rem;
        }

        .hero-mockup-inner {
          position: absolute;
          top: 10px;
          left: 50%;
          height: 92%;
          width: 69%;
          transform: translateX(-52%);
          overflow: hidden;
          border-radius: 35px;
          z-index: 1;
        }

        .hero-mockup-inner img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          object-position: 50% 0%;
        }

        .hero-mockup > img {
          position: relative;
          z-index: 10;
          width: 100%;
          height: auto;
        }

        @media (min-width: 1024px) {
          .hero-container {
            flex-direction: row !important;
            gap: 2.5rem !important;
          }

          .hero-text {
            width: 66.666% !important;
          }

          .hero-mockup {
            width: 33.333% !important;
            max-width: 450px !important;
            margin-top: 0 !important;
          }
        }
      `}</style>

      <Menu />
      <ThemeToggle />
      
      <div className="content">
        <h1>At Novus, we shape tomorrow.</h1>
      </div>

      {/* Spacer */}
      <div style={{ position: 'relative', zIndex: 4, minHeight: '10vh' }}></div>

      {/* Hero Section */}
      <section className="hero-section">
        <div className="hero-container">
          <div className="hero-text">
            <h2>
              <span>Business is about strategy,</span><br />
              <span className="hero-subheading"> not bureaucracy.</span>
            </h2>
            <p className="hero-description">
              Stop letting manual workflows eat your margins and energy. We engineer the silent automation systems that free you to focus on what actually matters: growth and innovation.
            </p>
            <div className="hero-buttons">
              <a href="/partnerships" className="hero-btn-primary">
                <svg style={{ width: '16px', height: '16px' }} fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M7 17L17 7M17 7H7M17 7V17"/>
                </svg>
                <span>Reclaim Your Time</span>
              </a>
              <a href="/about" className="hero-btn-secondary">
                Our philosophy
              </a>
            </div>
          </div>
          
          <div className="hero-mockup">
            <div className="hero-mockup-inner">
              <img src="/novus-phone-screen.png" alt="Novus System Notifications" />
            </div>
            <img src="https://deifkwefumgah.cloudfront.net/shadcnblocks/block/mockups/phone-2.png" alt="iPhone Mockup" />
          </div>
        </div>
      </section>

      {/* Extended scroll area - transition space */}
      <div style={{ position: 'relative', zIndex: 4, minHeight: '10vh' }}></div>

      {/* Process Section with Inverted Colors */}
      <Process />

      {/* Stats Section */}
      <Stats12 />

      {/* FAQ Section */}
      <Faq5 />

      {/* Footer */}
      <Footer />
    </>
  );
}
